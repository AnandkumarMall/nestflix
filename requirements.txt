fastapi==0.115.6
uvicorn[standard]==0.34.0
httpx==0.28.1
guessit==3.8.0
scikit-learn==1.6.1
numpy==2.2.1
python-dotenv==1.0.1
pytest==8.3.5
ruff==0.6.8

# System dependency (NOT pip): ffmpeg + ffprobe enable remux/transcode of containers and
# codecs browsers can't play natively (mkv, HEVC, AC3) and embedded-subtitle extraction.
# Optional — without it, natively-playable files still stream. Install e.g.:
#   winget install Gyan.FFmpeg   (Windows)   |   apt install ffmpeg / brew install ffmpeg
# Override discovery with FFMPEG_PATH / FFPROBE_PATH in .env.
